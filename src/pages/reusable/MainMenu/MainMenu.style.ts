import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  box: {
    margin: "5px",
    display: "flex",
    flexDirection: "column",
    height: "80vh",
  },
  menuIcon: {
    fontSize: "40px",
  },
  profilMenuList: {
    margin: "auto 0 0 0",
  },
});
